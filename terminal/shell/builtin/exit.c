#include "exit.h"

/**
** @brief               Exit main function.
** @return              Returns 0 in case of no problems.
*/
int exit(void)
{
    // free variables
    // terminal return value (error or not)
    return 0;
}
